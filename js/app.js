// js/app.js — boots index.html and owns screen switching. DOM only. See docs/05-ui-spec.md.
// Placeholder export; real surface defined in T9.

/** Boot the student app: read the pack from the hash, wire the screens, restore any draft.
 * @returns {void} */
export function boot() {
  throw new Error('app: not implemented');
}
