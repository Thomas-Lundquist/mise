// js/ui-review.js — Screen 3 (your plan). DOM only. See docs/05-ui-spec.md Screen 3.
// Placeholder export; real surface defined in T12.

/** Mount Screen 3 into a container.
 * @param {HTMLElement} root @param {object} ctx shared app context (pack, plan, schedule, callbacks) @returns {void} */
export function mount(root, ctx) {
  throw new Error('ui-review: not implemented');
}
