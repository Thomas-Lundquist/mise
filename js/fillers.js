// js/fillers.js — derive washables and fill idle gaps. Pure: no DOM.
// See docs/04-scheduler-spec.md Stage 4. Real logic lands in T6.

/** Fill idle gaps with derived washables and generic fillers.
 * Returns a new Schedule and never changes makespanMin.
 * @param {object} schedule @param {object} pack @param {object} plan @returns {object} a new Schedule */
export function fillGaps(schedule, pack, plan) {
  throw new Error('fillers: not implemented');
}
