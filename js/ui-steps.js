// js/ui-steps.js — Screen 2 (steps). DOM only. See docs/05-ui-spec.md Screen 2.
// Placeholder export; real surface defined in T11.

/** Mount Screen 2 into a container.
 * @param {HTMLElement} root @param {object} ctx shared app context (pack, plan, callbacks) @returns {void} */
export function mount(root, ctx) {
  throw new Error('ui-steps: not implemented');
}
