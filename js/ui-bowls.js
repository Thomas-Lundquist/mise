// js/ui-bowls.js — Screen 1 (bowls). DOM only. See docs/05-ui-spec.md Screen 1.
// Placeholder export; real surface defined in T10.

/** Mount Screen 1 into a container.
 * @param {HTMLElement} root @param {object} ctx shared app context (pack, plan, callbacks) @returns {void} */
export function mount(root, ctx) {
  throw new Error('ui-bowls: not implemented');
}
