// js/ui-author.js — teacher pack builder UI. DOM only. See docs/05-ui-spec.md teacher section.
// Placeholder export; real surface defined in T8.

/** Mount the teacher pack builder into a container.
 * @param {HTMLElement} root @param {object} ctx shared app context @returns {void} */
export function mount(root, ctx) {
  throw new Error('ui-author: not implemented');
}
